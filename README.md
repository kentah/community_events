This is set up to use mongolab to house the mongo database. In config.js, the user must change the 'mongodb://<...>' allocation to the true database location. companyName, etc, should also supplied by the user.
